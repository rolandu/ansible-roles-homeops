# roles/ssh_users/defaults/main.yml

# array of users to create/manage
ssh_users: []

# SSH key generation defaults
# - keys are generated per user at ~/.ssh/<ssh_users_key_filename>
# - public keys are exported to the controller under ssh_users_key_local_dir
ssh_users_generate_keys: true
ssh_users_key_type: ed25519
ssh_users_key_filename: id_ed25519
ssh_users_key_local_dir: "{{ inventory_dir }}/artifacts/ssh_keys"

# name of "ops" user for use by ansible
# - must be present in the users array
# - user will be set up with passwordless sudo
# - ignored if set to null.
ops_user: null

# Hardening options:
# Path to sshd_config (Debian/Ubuntu default)
sshd_config_path: /etc/ssh/sshd_config
# If true, we’ll disable password auth for SSH
ssh_disable_password_auth: true
# If true, we’ll disable root login
ssh_disable_root_login: true
